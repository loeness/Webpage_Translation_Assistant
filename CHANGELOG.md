# Changelog

本项目的所有重要变更都会记录在此文件中。

格式参考 Keep a Changelog，并遵循语义化版本（SemVer）。

## [1.5.2] - 2026-09-08

### Changed

- 用自定义内联 `btv-sentence` 元素代替通用 `span`，降低站点 CSS 改变句子布局的概率。
- 原文浮窗支持内部滚动、文本选择、复制和 Esc 关闭，并限制在当前视口内。
- 弹窗增加页面状态、明确重新捕获和恢复页面结构操作。

### Fixed

- 动态流式追加文本按最小块重建句子记录，避免只保存半句。
- 普通业务文本变化进入不确定状态，不再把旧原文显示给新内容；用户可在确认当前为原文后显式重新捕获。
- SPA 路由切换使用新的记录代次，上一页面的 DOM 记录不能在新页面复用。
- 任一原句片段或受保护的公式上下文失效时，整条原文记录一并失效。
- 划词结果会标出没有原文记录的部分，不再静默省略。
- 修正弹窗恢复与重新捕获按钮错误发送预处理消息的问题。

### Validation

- 真实 Chromium 回归覆盖长页面、极端译文长度、跨标签句子、流式内容、业务更新、SPA、选区缺失、结构恢复和弹窗协议。

## [1.5.1] - 2026-09-07

### Changed

- Edge/Chrome 共用 `content.core.js`，用翻译前的句子 DOM 标记直接关联原文，替换按译文长度、句序和页面坐标猜测的映射。
- 预处理覆盖整个已加载文档，完成所有句子标记后才返回完成状态，不再将后台排队视为完成。
- 保留行内格式和链接元素；同一句跨多个文本节点的片段共享一条原文记录。
- 移除自动滚动探测，避免改变用户滚动位置或触发额外加载。

### Fixed

- 修复长文档下半部分未捕获原文便开始翻译的问题。
- 修复译文长度变化或拆句导致的相邻原句错位。
- 翻译后重复预处理拒绝覆盖原文，关闭再开启保留已有记录。
- 划词按实际字符交集聚合，排除仅触及开头的下一句。
- 不再通过旧 DOM 路径或复制的属性给重建节点匹配原文。

### Validation

- 新增真实浏览器 DOM 回归测试，模拟翻译器文本替换及包装；不等同于浏览器在线翻译服务的端到端测试。

## [1.1.0] - 2026-03-27

### Added

- 支持对翻译后页面进行原文预处理和映射。
- 支持点击单句展示对应原文。
- 支持划词后聚合展示原文片段。
- 新增生产级目录结构（assets/src/_locales）。
- 新增多语言扩展元信息（en 与 zh_CN）。
- 新增扩展图标资源（16/48/128）。
- 新增 README、LICENSE、发布模板与 Git 忽略规则。

### Changed

- 将 popup 样式从 HTML 内联拆分到独立文件。
- 调整 manifest 路径引用以匹配新目录布局。

### Fixed

- 修复内容脚本未注入时的消息连接失败问题（自动注入兜底）。
- 修复 Tooltip 原文可能被浏览器翻译器二次翻译的问题。

## [1.3.0] - 2026-03-29

### Added
- **底层架构重构**：从单节点扫描升级为“块级逻辑聚合”模型，彻底解决加粗、代码块、链接等行内标签导致的语义断裂问题。
- **虚拟长字符串投影 (Virtual Projection)**：引入实时块文本投影技术，支持 `<strong>`、`<code>`、`<a>` 标签混排下的完整原文映射。
- **绝对偏移映射系统**：通过 `TreeWalker` 实时计算文本节点在块内的绝对坐标，大幅提升点击判定精度。
- **非强制快照冻结**：增加快照保护机制，防止浏览器翻译后的 DOM 变动污染原始快照数据。

### Fixed
- 修复了在 GitHub、CSDN 等代码高亮页面中，由于 `<code>` 标签导致的原文显示不全或断句错误。
- 优化了置信度回退逻辑，当映射偏移异常时，自动回退至更高层级的块级原文，保证显示的稳定性。
